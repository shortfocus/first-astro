---
import { siteConfig } from "@/config/site.config";
const year = new Date().getFullYear();
---

<footer class="border-t border-slate-800 bg-slate-950">
  <div class="mx-auto max-w-6xl px-6 py-12">
    {/* Top */}
    <div class="grid gap-10 md:grid-cols-3">
      {/* Brand */}
      <div>
        <h3 class="text-lg font-bold text-white">
          {siteConfig.name}
        </h3>
        <p class="mt-3 text-sm text-slate-400 leading-relaxed">
          {siteConfig.description}
        </p>
      </div>

      {/* Services */}
      <div>
        <h4 class="mb-4 text-sm font-semibold text-slate-200">서비스</h4>
        <ul class="space-y-2 text-sm">
          <li>
            <span class="text-slate-400">기업 홈페이지 제작</span>
          </li>
          <li>
            <span class="text-slate-400">랜딩페이지 제작</span>
          </li>
          <li>
            <span class="text-slate-400">유지보수 없는 정적 사이트</span>
          </li>
        </ul>
      </div>

      {/* Contact */}
      <div>
        <h4 class="mb-4 text-sm font-semibold text-slate-200">문의</h4>
        <ul class="space-y-3 text-sm text-slate-400">
          <li>이메일: contact@solidweb.kr</li>
          <li>
            <a
              href="/contact"
              class="inline-block rounded-md bg-sky-500 px-4 py-2 text-sm font-semibold text-white hover:bg-sky-400 transition"
            >
              상담 요청
            </a>
          </li>
        </ul>
      </div>
    </div>

    {/* Bottom */}
    <div class="mt-12 border-t border-slate-800 pt-6 text-sm text-slate-500">
      © {year}. {siteConfig.name}. All rights reserved.
    </div>
  </div>
</footer>
